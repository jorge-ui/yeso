{
  "name": "yeso",
  "version": "0.0.1",
  "description": "",
  "keywords": "yeso",
  "homepage": "https://jorge-ui.github.io/yeso",
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html",
    "build:prod": "parcel build --public-url=/yeso index.html",
    "dev": "parcel index.html --open",
    "deploy": "node deploy.js",
    "predeploy": "npm run build:prod",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.54.5"
  },
  "dependencies": {
    "date-fns": "^2.29.2"
  }
}
